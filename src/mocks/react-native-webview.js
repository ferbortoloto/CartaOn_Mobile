import React, { forwardRef } from 'react';
import { View } from 'react-native';

const WebView = forwardRef(function WebView({ style }, _ref) {
  return <View style={style} />;
});

export default WebView;
